dnb.Canvas.createSubclass("InteractionCanvas", {
	controller 			: null,
/*
	onMouseDown : function(event) {
		if (this.controller.tool && this.controller.tool.onMouseDown) {
			this.controller.tool.onMouseDown(event);
		}
	},
	
	onMouseMove : function(event) {
		if (this.controller.tool && this.controller.tool.onMouseMove) {
			this.controller.tool.onMouseMove(event);
		}
	},
	
	onMouseUp : function(event) {
		if (this.controller.tool && this.controller.tool.onMouseUp) {
			this.controller.tool.onMouseUp(event);
		}
	},
	
	onDoubleClick : function(event) {
		if (this.controller.tool && this.controller.tool.onDoubleClick) {
			this.controller.tool.onDoubleClick(event);
		}
	}
*/
});
